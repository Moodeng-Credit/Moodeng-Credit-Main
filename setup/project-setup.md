Instructions if projects' ownerships are transferred:
1. Publish edge functions
2. Supabase db push
3. Update the env file accordingly 
4. Update the secrets in Supabase Edge Functions (run the `sync-secrets-*.sh` script in `setup` folder)
5. Reset the DB
