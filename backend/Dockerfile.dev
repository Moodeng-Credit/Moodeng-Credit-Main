FROM node:24-alpine

WORKDIR /app

# Install nodemon for hot reload when files change (volume-mounted source)
RUN npm install -g nodemon

# Copy backend files
COPY . .

# Expose port 8000
EXPOSE 8000

# Run with nodemon so server restarts on file changes
CMD ["nodemon", "server.js"]
